import { cutToolBar } from "./cut";
import { remixToolBar } from "./remix";
import { sendToolBar } from "./send";
import { upscaleToolBar } from "./upscale";

export const toolbars = [cutToolBar, remixToolBar, upscaleToolBar, sendToolBar];
